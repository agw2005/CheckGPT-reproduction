Contains checkpoints and attempts at reproduction before the final pipeline was created.

Do note that these checkpoints contains mistakes that are fixed in the `run_reproduction.ipynb`.

Nevertheless, I opt to make these checkpoints available instead of deleting them entirely.